VAULTWARDEN_DOMAIN=https://vault.yourdomain.com

DOCKER_VOLUME_DIR=/var/lib/vaultwarden
VAULTWARDEN_DATA_VOLUME=${DOCKER_VOLUME_DIR}/data

# Memory limit (auto-calculated by generate-passwords.sh; override if needed)
#VAULTWARDEN_MEMORY_LIMIT=256m
